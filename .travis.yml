sudo: required

services:
  - docker

before_install:
  - docker build -t diksha/docker-react-transci-aws -f Dockerfile.dev .

script:
  - docker run  diksha/docker-react-transci-aws npm run test -- --coverage
